[project]
name = "emba-api"
version = "0.1.0"
description = "RESTful API for EMBA firmware security analyzer"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "python-multipart>=0.0.18",
]

[project.scripts]
emba-api = "main:app"
